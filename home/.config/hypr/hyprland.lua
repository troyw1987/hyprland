---@module 'hl'

-- Settings (order mirrors the old settings/*.conf load order: autostart, env, general, keybinds, monitors, style)
require("settings.autostart")
require("settings.env")
require("settings.general")
require("settings.keybinds")
require("settings.monitors")
require("settings.style")
